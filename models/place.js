class Place {
  constructor(id, title, imageUri, address, lat, lng, note) {
    this.id = id;
    this.title = title;
    this.imageUri = imageUri;
    this.address = address;
    this.lat = lat;
    this.lng = lng;
    this.note = note;
  }
}

export default Place;
